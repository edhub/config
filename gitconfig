[color]
    branch = auto
    diff = auto
    status = auto
    showbranch = auto
    ui = true
[core]
    editor = nvim
    quotepath = false
[user]
    name = Edward Gu
    email = edhroyal@gmail.com

# Special treat for rust update
[url "https://github.com/rust-lang"]
    insteadOf = "https://github.com/rust-lang"
# Use git protocal by default
[url "git@github.com:"]
    insteadOf = "https://github.com/"

[alias]
    st = status
    cm = commit
    cma = commit --amend
    br = branch -v
    cp = cherry-pick
    co = checkout
    df = diff -C
    dfc = diff --cached
    showl = show --name-status -C
    rmt = remote -v
    gl = log --graph --all --abbrev-commit --relative-date --date=short  --format=\"%x09 %h %Cgreen%cd%Creset [%Cblue%cn%Creset] %C(auto)%d%Creset %s\"
