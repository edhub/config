[color]
    ui = auto
[alias]
    st = status
    cm = commit
    cma = commit --amend
    br = branch -v
    cp = cherry-pick
    co = checkout
    df = diff -C
    dfc = diff --cached -C
    showl = show --name-only
    rmt = remote -v
[core]
    editor = vim
